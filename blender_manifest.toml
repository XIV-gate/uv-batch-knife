schema_version = "1.0.0"

id = "uv_batch_knife"
version = "1.5.9"
name = "UV Batch Knife"
tagline = "Cut overlapping UV islands with one UV-space line"
maintainer = "XIVgate"
type = "add-on"
website = "https://github.com/XIV-gate/uv-batch-knife"

blender_version_min = "5.0.0"

license = [
  "SPDX:GPL-3.0-or-later",
]

[permissions]
network = "Check GitHub Releases and download plugin updates"
files = "Store downloaded update archives in a temporary folder"

[build]
paths = [
  "__init__.py",
  "README.md",
]
